# ============================================
# Configuración Docker para Desarrollo
# ============================================
# Este archivo contiene variables de entorno para desarrollo con Docker
# Copia este archivo a .env y ajusta los valores según necesites
#
# Uso:
#   cp docker.env.example .env
#   # Edita .env con tus valores

# Base de Datos MySQL
MYSQL_ROOT_PASSWORD=rootpassword_dev_change_me
MYSQL_DATABASE=gestor_proyectos
MYSQL_USER=gestor_user
MYSQL_PASSWORD=gestor_password_dev

# Backend API
JWT_SECRET=dev_jwt_secret_change_in_production_please
FRONTEND_URL=http://localhost:4321

# GitHub OAuth (Opcional - dejar vacío si no se usa)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_CALLBACK_URL=http://localhost:4321/api/integrations/github/oauth/callback
# Generar con: openssl rand -base64 32
GITHUB_TOKEN_ENC_KEY=

# ============================================
# FEATURE FLAGS - Community vs Premium (Open-Core)
# ============================================
# 
# IMPORTANTE: Para usar Premium Edition, necesitas:
# 1. Clonar con submodule: git clone --recursive <repo-url>
# 2. O inicializar submodule: git submodule update --init --recursive
# 3. Activar FEATURE_PREMIUM=true y configurar flags individuales
#
# Edición de la aplicación: 'community' o 'premium' (default: community)
APP_EDITION=community

# Habilitar módulo premium (requiere submodule premium disponible)
# Si es true y el submodule existe, se intentará cargar el módulo premium
FEATURE_PREMIUM=false

# Habilitar todas las features premium (si APP_EDITION=premium y FEATURE_PREMIUM=true)
# Si es true, todas las features premium estarán habilitadas
PREMIUM_FEATURES=false

# Features individuales (nuevos nombres con prefijo FEATURE_)
# Si PREMIUM_FEATURES=true, estos flags se ignoran
FEATURE_GITHUB=false
FEATURE_ROADMAP=false
FEATURE_GANTT=false
FEATURE_RELEASES=false
FEATURE_MULTI_TENANT=false
FEATURE_SUPER_ADMIN=false
FEATURE_BUDGETS=true
FEATURE_RATE_CARDS=true
FEATURE_RELEASE_BUDGETS=false

# Features individuales (nombres legacy - compatibilidad)
# Se soportan ambos nombres (FEATURE_* y ENABLE_*)
ENABLE_GITHUB=false
ENABLE_ROADMAP=false
ENABLE_GANTT=false
ENABLE_RELEASES=false
ENABLE_MULTI_TENANT_DASHBOARD=false
ENABLE_SUPER_ADMIN_UI=false
PREMIUM_BUDGETS=true
PREMIUM_RATE_CARDS=true
PREMIUM_RELEASE_BUDGETS=false

# Variables de entorno públicas para el frontend (prefijo PUBLIC_)
# Estas se exponen al cliente y deben coincidir con las del backend
PUBLIC_APP_EDITION=community
PUBLIC_FEATURE_PREMIUM=false
PUBLIC_PREMIUM_FEATURES=false

# Features individuales frontend (nuevos nombres)
PUBLIC_FEATURE_GITHUB=false
PUBLIC_FEATURE_ROADMAP=false
PUBLIC_FEATURE_GANTT=false
PUBLIC_FEATURE_RELEASES=false
PUBLIC_FEATURE_MULTI_TENANT=false
PUBLIC_FEATURE_SUPER_ADMIN=false
PUBLIC_FEATURE_BUDGETS=true
PUBLIC_FEATURE_RATE_CARDS=true
PUBLIC_FEATURE_RELEASE_BUDGETS=false

# Features individuales frontend (nombres legacy - compatibilidad)
PUBLIC_ENABLE_GITHUB=false
PUBLIC_ENABLE_ROADMAP=false
PUBLIC_ENABLE_GANTT=false
PUBLIC_ENABLE_RELEASES=false
PUBLIC_ENABLE_MULTI_TENANT_DASHBOARD=false
PUBLIC_ENABLE_SUPER_ADMIN_UI=false
PUBLIC_PREMIUM_BUDGETS=true
PUBLIC_PREMIUM_RATE_CARDS=true
PUBLIC_PREMIUM_RELEASE_BUDGETS=false

# ============================================
# NOTAS:
# ============================================
# 1. En Docker, la API usa: DATABASE_URL=mysql://root:${MYSQL_ROOT_PASSWORD}@db:3306/${MYSQL_DATABASE}
# 2. El frontend usa API_URL=http://api:3001 (nombre del servicio en Docker)
# 3. NO incluyas secretos reales en este archivo
# 4. Para producción, usa variables de entorno del sistema o un gestor de secretos
# 5. Para activar Premium Edition:
#    - Cambia APP_EDITION=premium
#    - Cambia PUBLIC_APP_EDITION=premium
#    - Activa PREMIUM_FEATURES=true o activa features individuales
#
# 6. Feature Flags de Presupuestos (Premium):
#    - PREMIUM_BUDGETS=true: Habilita gestión de presupuestos por proyecto/sprint/release
#    - PREMIUM_RATE_CARDS=true: Habilita tarifas por rol/usuario
#    - PREMIUM_RELEASE_BUDGETS=true: Habilita presupuestos por release (requiere FEATURE_RELEASES=true)
#    - También puedes usar FEATURE_BUDGETS, FEATURE_RATE_CARDS, FEATURE_RELEASE_BUDGETS

